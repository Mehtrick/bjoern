dependencies{
    api project(":bjoern-parser")
	api 'junit:junit:4.12'
	api 'org.junit.jupiter:junit-jupiter-engine:5.4.0'
}

jar {
	archivesBaseName = "bjoern-base-generator"
	version = project.version
}
